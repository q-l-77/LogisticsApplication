# Copy this file to .env and fill in your key to enable the chat assistant.
# The structured quote form works without this — only /api/chat needs it.
ANTHROPIC_API_KEY=sk-ant-...

# Optional
PORT=3000
