{
  "name": "rate-buddy-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rate Buddy — shipping quote & rules-explainer prototype backend.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test src/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=18.20.0"
  }
}
